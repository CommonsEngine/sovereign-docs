{
  "name": "@sovereign/docs",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "echo \"Nothing to build yet\"",
    "lint": "eslint . || true",
    "test": "node --test || true"
  }
}
